import{_ as p,p as t,q as e,a1 as o}from"./framework-5866ffd3.js";const a={},h=o('<p>SVN，HG，GIT命令对照</p><p>SVN，HG，GIT命令对照 SVN，HG，GIT是三种常见的版本控制系统，本文简单列出一些相似的操作命令。当然，三种系统各有特点，严格的对应关系是没有的。 1，第一次下载，包括源码和版本库： svn checkout http://path/to/repo repo_name hg clone http://path/to/repo repo_name git glone http://path/to/repo repo_name或者</p><p>SVN，HG，GIT命令对照 SVN，HG，GIT是三种常见的版本控制系统，本文简单列出一些相似的操作命令。当然，三种系统各有特点，严格的对应关系是没有的。</p><p>1，第一次下载，包括源码和版本库：</p><p>svn checkout http://path/to/repo repo_name</p><p>hg clone http://path/to/repo  repo_name</p><p>git clone http://path/to/repo repo_name或者git clone git://path/to/repo repo_name</p><p>2，下载服务器上最新的更新：</p><p>svn update</p><p>hg pull &amp;&amp; hg update -C</p><p>git pull</p><p>3，检出某个修订版本</p><p>svn checkout -r &lt;rev&gt;</p><p>hg update -C -r &lt;rev&gt;</p><p>git reset –hard -r &lt;rev&gt;</p><p>4，新增被跟踪文件</p><p>svn add /path/to/file</p><p>hg add /path/to/file</p><p>git add /path/to/file</p><p>4，移除被跟踪文件</p><p>svn rm /path/to/file</p><p>hg remove /path/to/file</p><p>git rm /path/to/file</p><p>5，生成补丁</p><p>svn diff  &gt;patch_file</p><p>hg diff &gt;patch_file</p><p>git diff &gt;patch_file</p><p>6，提交更改</p><p>svn commit</p><p>hg commit</p><p>git commit</p><p>6，查看当前状态</p><p>svn info</p><p>hg status</p><p>git status</p><p>7，查看修订记录</p><p>svn log</p><p>hg log</p><p>git log</p><p>8，启动服务器</p><p>svnserve -d</p><p>hg serve -p 8002 &amp;</p><p>git daemon –base-path=/path/to/repo –export-all &amp;</p><p>通过<a href="http://tech.16c.cn/svnsy/20100112/17492.html">SVN，HG，GIT命令对照 - 联城技术网</a>.</p>',44),r=[h];function n(i,l){return t(),e("div",null,r)}const g=p(a,[["render",n],["__file","2011-146.html.vue"]]);export{g as default};
